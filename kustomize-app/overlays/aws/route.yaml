apiVersion: route.openshift.io/v1
kind: Route
metadata:
  labels:
    run: apache-service
  name: apache-service
  namespace: aws-apache-test
spec:
  host: apache-service-aws-apache-test.apps.scollier-spoke-aws.scollier.sysdeseng.com
  port:
    targetPort: 8080
  to:
    kind: Service
    name: apache-service
    weight: 100
  wildcardPolicy: None
